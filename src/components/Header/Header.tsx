import React from 'react';
import styles from './Header.module.css';

export function Header() {
  return (
    <header className={styles.headerContainer}>
      { 
        /*  logo icone de bateria, etc.*/
      }
    </header>
  );
}

export default Header;